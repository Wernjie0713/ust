{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "scan:banned": "node -e \"const fs=require('fs');const {execSync}=require('child_process');const files=execSync('git ls-files -z').toString().split('\\0').filter(Boolean).filter(f=>!f.includes('package.json')&&!f.includes('package-lock.json')&&!f.includes('reports/')&&!f.includes('node_modules/')&&!f.includes('.mp4')&&!f.includes('.png')&&!f.includes('.jpg')&&!f.includes('.jpeg')&&!f.includes('.gif')&&!f.includes('.svg')&&!f.includes('.ico'));const bad=/(iota|tangle|firefly|shimmer|hornet|stardust|wallet\\.rs|identity\\.rs|streams|\\bdid\\b(?!\\s+you\\s+know))/i;const hits=[];for(const f of files){try{const t=fs.readFileSync(f,'utf8');if(bad.test(t))hits.push(f);}catch{}}if(hits.length){console.error('BANNED TERMS FOUND:\\n'+hits.join('\\n'));process.exit(1)}console.log('No banned terms.')\"",
    "smoke": "node scripts/smoke.js",
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint"
  },
  "dependencies": {
    "formidable": "^3.5.4",
    "mapbox-gl": "^3.15.0",
    "next": "15.5.3",
    "react": "19.1.0",
    "react-dom": "19.1.0",
    "react-icons": "^5.5.0",
    "threebox-plugin": "^2.2.7"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "15.5.3",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
